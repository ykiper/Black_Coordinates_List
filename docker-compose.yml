services:
  redis:
    image: "redis:alpine"
    volumes:
      - redis_data:/data
  
  service-b:
    build:
      context: ./service-b
      dockerfile: app/Dockerfile
    env_file:
      - .env.example
    ports:
      - "8001:8001"
    depends_on:
      - redis

volumes:
  redis_data: